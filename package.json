{
  "name": "employee-management",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix apps/api && npm install --prefix apps/web && npm run db:generate --prefix apps/api",
    "dev": "npm run dev --prefix apps/api & npm run dev --prefix apps/web & wait",
    "dev:api": "npm run dev --prefix apps/api",
    "dev:web": "npm run dev --prefix apps/web",
    "db:migrate": "npm run db:deploy --prefix apps/api",
    "db:seed": "npm run db:seed --prefix apps/api",
    "build": "npm run build --prefix apps/api && npm run build --prefix apps/web",
    "test": "npm test --prefix apps/api && npm test --prefix apps/web",
    "typecheck": "npm run typecheck --prefix apps/api && npm run typecheck --prefix apps/web"
  }
}
